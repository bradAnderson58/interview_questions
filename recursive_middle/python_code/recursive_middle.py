from typing import TypeVar, Optional
from linked_list import Node, Linked

T = TypeVar('T')

'''
Implement the following method using recursive calls only (no loops)

find_recursive_middle:
Finds the middle element of a linked list.  For even number lists, it rounds up.
Args:
    linked:  The linked list of type T to find the middle element of

Returns:
    the element of type T in the middle of the list, or None if the list is invalid
'''

def find_recursive_middle(linked: Linked[T]) -> Optional[T]:
    pass

